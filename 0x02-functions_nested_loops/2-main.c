#include "main.h"

/**
 * main - check the code description
 * Description : a function that prints 10 times the alphabet, in lowercase,
 * followed by a new line.
 * Return: Always 0.
 */
int main(void)
{
	print_alphabet_x10();
	return (0);
}
