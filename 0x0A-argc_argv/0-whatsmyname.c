#include <stdio.h>
#include <stdlib.h>
/**
 * main - progrram entry point
 * @argc: argument count
 * @argv: argument value
 *
 * Return: Always 0.
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
